spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/delivery_db
    username: u0_a758
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379

kafka:
  bootstrap-servers: localhost:9092
