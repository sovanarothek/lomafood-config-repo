server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lomafood
    username: postgres
    password: roth2025k@1

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

jwt:
  secret: lomafood-secret-key
  expiration: 900000   # 15 minutes
