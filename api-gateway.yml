server:
  port: 8080

spring:
  cloud:
    gateway:
routes:
        - id: test-route
          uri: https://httpbin.org
          predicates:
            - Path=/test/**
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
